[URLs]
token_url = https://example.com/get-token

[Settings]
app_name = My Application
version = 1.0.0

[Dependencies]
# Additional dependencies to install (comma-separated)
# The parent folder will be automatically installed in editable mode
#additional_packages = requests, pytest
